export type SupportedLanguage = "English" | "French" | "German" | "Spanish";

export type DashboardCopy = {
  eyebrow: string;
  title: string;
  body: string;
  sync: string;
};

export type BookingPrices = Record<string, number>;
